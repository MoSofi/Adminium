name: e2e

# Stub. Filled in at M4+ (workplan/01-architecture.md §11, task 01-T10):
# Playwright against the built server + dashboard on the sqlite / postgres:16 /
# mysql:8.4 service-container matrix, seeded demo data, trace upload on failure.

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Explain stub
        run: echo "e2e.yml is a stub. It is implemented in milestone M4+ (task 01-T10) — Playwright suite on the sqlite/postgres/mysql matrix."
