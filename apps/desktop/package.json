{
  "name": "@adminium/desktop",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "Electron offline desktop app: @adminium/server in a utilityProcess + the dashboard build on loopback",
  "homepage": "https://adminium.dev",
  "author": "Adminium <hello@adminium.dev>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MoSofi/Adminium.git",
    "directory": "apps/desktop"
  },
  "main": "out/main/index.js",
  "//exports": [
    "ONE public entry point, and it ships no code (11-electron.md \u00a74): 'Typed",
    "contract lives in apps/desktop/src/preload/api.d.ts and is re-exported to the",
    "dashboard as @adminium/desktop/api (types only) so @adminium/dashboard compiles",
    "without Electron installed.'",
    "",
    "The `types` condition is the ONLY one, deliberately. A subpath with no runtime",
    "target cannot be imported at runtime \u2014 Node and every bundler fail the resolve",
    "with ERR_PACKAGE_PATH_NOT_EXPORTED rather than pulling an Electron app into a",
    "browser bundle. That is the 01-architecture.md \u00a72.3 rule ('apps are not imported')",
    "enforced by the resolver instead of by review.",
    "",
    "The root '.' is absent for the same reason: this app is not a library. Electron",
    "loads `main` by absolute path (it never resolves @adminium/desktop as a",
    "specifier), so `main` and `exports` are not in competition here.",
    "",
    "NOTE \u2014 no package depends on this one, and none may: @adminium/desktop#build",
    "dependsOn @adminium/dashboard#build (turbo.json), and turbo builds its graph",
    "from `dependencies` AND `devDependencies`. A dashboard \u2192 desktop package edge",
    "would therefore close a task cycle and every `turbo run` in the repo would fail.",
    "@adminium/dashboard consumes this contract through a tsconfig `paths` mapping to",
    "this exact file instead \u2014 a type-only edge with no package edge. See the note in",
    "apps/dashboard/tsconfig.json."
  ],
  "exports": {
    "./api": {
      "types": "./src/preload/api.d.ts"
    }
  },
  "//scripts": [
    "`build` chains the offline asset gate because 11-electron.md \u00a77 says it 'runs",
    "in CI on every desktop build' \u2014 and the only way to make that literal, rather",
    "than a workflow step someone can forget to keep next to the build, is to make",
    "it part of the build. It scans out/renderer + out/dashboard, which do not",
    "exist until electron-vite has run, so this is also the earliest point it CAN",
    "run. The root `pnpm check-offline-assets` script is the same check as a named",
    "CI step (matching check-deps); both are wanted."
  ],
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build && node ../../scripts/generate-notices.mjs && node ../../scripts/check-offline-assets.mjs",
    "preview": "electron-vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "icons": "node ../../scripts/make-icons.mjs",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "//packaging": [
    "11-T13. `dist`/`pack` are the ONLY call sites of electron-builder.yml \u2014 a",
    "config no script runs packages nothing. They are NOT turbo tasks: turbo's",
    "cache keys off file inputs, and a real electron-builder run downloads Electron",
    "binaries and (on mac) reads signing certs from the environment, neither of",
    "which is a hermetic input turbo can hash. The release workflow (11-T14) drives",
    "the full sequence explicitly, in order:",
    "",
    "  1. build   \u2014 electron-vite bundles out/**, generates NOTICES, offline-gate",
    "  2. icons   \u2014 derive resources/icons/* from the master (needs the sharp +",
    "               icon-gen devDeps; see below). electron-builder.yml points",
    "               mac/win/linux .icon at these files, so this runs BEFORE dist.",
    "  3. dist    \u2014 electron-builder: npmRebuild the native addons for Electron's",
    "               ABI (in apps/desktop/node_modules \u2014 see //native-modules),",
    "               then sign/notarize/package.",
    "",
    "`pack` (--dir, no installers, no signing) is the fast local smoke: it proves",
    "the app dir assembles and the native rebuild took, without the dmg/notarize",
    "cost. Both `pack` and `dist` leave the pnpm-shared better-sqlite3/argon2",
    "addons built for Electron's ABI, so LOCALLY they must be followed by `pnpm",
    "rebuild better-sqlite3 argon2` at the repo root or the rest of the repo's",
    "tests go red. `sharp` and `icon-gen` are DECLARED devDependencies below (build-only):",
    "the release workflow runs `pnpm install --frozen-lockfile`, which installs",
    "only what the lockfile already carries \u2014 it can neither `pnpm add` on the fly",
    "nor would --frozen-lockfile permit it \u2014 so the `icons` step's `loadDesktopDep`",
    "resolve of each from apps/desktop MUST find them already linked here. They are",
    "not runtime deps of the shell and no `src/` file imports them; only",
    "scripts/make-icons.mjs (repo root) reaches them, via a require anchored at this",
    "package."
  ],
  "//dependencies": [
    "RUNTIME DEPS OF THE FORKED SERVER (11-electron.md \u00a72.1/\u00a73), not of the shell.",
    "electron.vite.config.ts bundles @adminium/server into out/server/index.js, but",
    "a bundler cannot inline a module reached through a COMPUTED specifier, and it",
    "must not inline a native one. Those stay runtime `import`s \u2014 resolved by Node",
    "against the IMPORTING file (out/server/index.js), i.e. against THIS package's",
    "node_modules, never against apps/server's. So every one of them is declared",
    "here even though no line of apps/desktop/src imports any of them. Verified by",
    "resolving each from out/server/index.js; a miss is silent at runtime, which is",
    "what makes the list load-bearing rather than decorative:",
    "",
    "  @adminium/adapter-{postgres,mysql,sqlite} \u2014 connections/register-adapters.ts",
    "    loads these by name from ADAPTER_PACKAGES and CATCHES the failure into",
    "    `missing: []`. Undeclared, the desktop app boots a complete API and then",
    "    cannot open a database of any engine \u2014 including the local SQLite the whole",
    "    product is built on \u2014 with no error anywhere but a debug log. Their own",
    "    header states the rule: declared deps of whichever package hosts the entry",
    "    file, 'so they resolve inside the deployed image and not just in a",
    "    workspace checkout'. out/server/index.js is a new such artifact.",
    "  better-sqlite3, argon2 \u2014 native (.node). argon2 is a static external; the",
    "    meta store reaches better-sqlite3 via `importDriver`. \u00a72.1 requires these",
    "    rebuilt against Electron's ABI with @electron/rebuild \u2014 SEE THE NOTE",
    "    BELOW, because WHERE that happens is not a free choice here.",
    "  pg, mysql2 \u2014 OPTIONAL, mirroring apps/server's own note. The desktop meta",
    "    store is ALWAYS local SQLite (\u00a72.1), but a SOURCE db may be a reachable",
    "    Postgres/MySQL, and meta/store.ts reaches those drivers the same way.",
    "",
    "This is a DEPLOYMENT surface, not an import edge: 01-architecture.md \u00a72.3 caps",
    "what apps/desktop/src may IMPORT at @adminium/server, and .dependency-cruiser",
    "`desktop-shell-only` enforces exactly that against the source graph."
  ],
  "dependencies": {
    "@adminium/adapter-mysql": "workspace:*",
    "@adminium/adapter-postgres": "workspace:*",
    "@adminium/adapter-sqlite": "workspace:*",
    "@adminium/server": "workspace:*",
    "argon2": "^0.45.1",
    "better-sqlite3": "^12.2.0",
    "croner": "^9.1.0",
    "electron-updater": "^6.8.9",
    "fflate": "^0.8.3",
    "zod": "catalog:"
  },
  "optionalDependencies": {
    "mysql2": "^3.23.1",
    "pg": "^8.16.3"
  },
  "//native-modules": [
    "NATIVE ABI: @electron/rebuild MUST NOT be wired into a `postinstall` here,",
    "and this note is why \u2014 the instruction in 11-electron.md \u00a72.1 ('wired into",
    "`postinstall` for `apps/desktop` and into electron-builder's `npmRebuild`')",
    "predates the pnpm layout and its first half is not safely expressible in it.",
    "",
    "pnpm gives the whole workspace ONE better-sqlite3: apps/server, apps/desktop,",
    "apps/e2e, packages/meta and packages/adapter-sqlite all symlink to the same",
    "node_modules/.pnpm/better-sqlite3@<v>/node_modules/better-sqlite3, and",
    "prebuild-install resolves a single build/Release/better_sqlite3.node inside",
    "it. electron-rebuild rewrites that file in place. So a postinstall in this",
    "package would compile the SHARED copy against Electron's ABI (148 for",
    "Electron 43) and every Node-side suite in the repo \u2014 which runs at the local",
    "Node's ABI (127 on Node 22) \u2014 would then fail to dlopen it. `pnpm test` at",
    "the root would be red for everyone, on every install, as a side effect of a",
    "package they are not touching.",
    "",
    "The rebuild therefore belongs in electron-builder's `npmRebuild: true`,",
    "wired with electron-builder.yml in 11-T13 (Packaging), together with \u00a710's",
    "`asarUnpack: ['**/*.node']`. That is the only half of \u00a72.1's sentence this",
    "layout permits, and it is the half that ships.",
    "",
    "BUT NOT BECAUSE IT TOUCHES A PRIVATE COPY \u2014 corrected 2026-07-21. This note",
    "and electron-builder.yml both used to justify that choice by claiming",
    "`npmRebuild` operates on a STAGED, private node_modules. It does not:",
    "app-builder-lib forks @electron/rebuild with `buildPath: appDir`, and appDir",
    "is apps/desktop itself, so the rebuild rewrites the SAME shared",
    "better_sqlite3.node described above. The real difference between the two",
    "sites is WHO PAYS and WHEN, and that difference still decides it: a",
    "postinstall corrupts the shared addon for everyone on every `pnpm install`,",
    "including people who never touch this package; `npmRebuild` only fires when",
    "someone deliberately runs `pack`/`dist`, and on CI that runner is discarded.",
    "The local cost is one command, documented in apps/desktop/README.md",
    "('Packaging locally') and in electron-builder.yml's header:",
    "",
    "    pnpm rebuild better-sqlite3 argon2     # repo root, after any pack/dist",
    "",
    "Making the comment literally true would mean packaging out of a `pnpm deploy",
    "--filter @adminium/desktop` tree (a real, non-symlinked node_modules) behind",
    "a beforeBuild hook \u2014 a rewiring of a release path that has never executed",
    "end to end, so it is a follow-up, not a v1 change.",
    "",
    "CONSEQUENCE FOR THE DEV LOOP, stated rather than hidden: until 11-T13 lands,",
    "`pnpm --filter @adminium/desktop dev` forks a utilityProcess on Electron's",
    "bundled Node and the meta store's better-sqlite3 import throws",
    "ERR_DLOPEN_FAILED (NODE_MODULE_VERSION mismatch), which bootDesktopServer",
    "reports as { type: 'error', stage: 'meta-store' } and the shell renders on",
    "the crash screen. @electron/rebuild is declared here so 11-T13 has it; the",
    "unit suites do not load native modules under Electron, which is why no gate",
    "in this wave observes any of this."
  ],
  "//devDependencies": [
    "@adminium/dashboard and @adminium/tokens are BUILD INPUTS, never imports",
    "(01-architecture.md \u00a72.3 lets desktop use `dashboard build output (static",
    "files)` and bans reaching into packages' internals; .dependency-cruiser.cjs",
    "`desktop-shell-only` enforces it). The dashboard's dist/ is copied to",
    "out/dashboard/ by electron.vite.config.ts; tokens supplies the CSS the",
    "boot/crash splash pages inline (\u00a72.2 step 6). The edges are declared so turbo",
    "orders dashboard#build and tokens#build before desktop#build.",
    "",
    "`sharp` + `icon-gen` are build-only deps of the `icons` step (see //packaging).",
    "They are declared here \u2014 not `pnpm add`ed on first use \u2014 so the release job's",
    "`pnpm install --frozen-lockfile` links them into apps/desktop where",
    "scripts/make-icons.mjs resolves them; a frozen install cannot add a package the",
    "lockfile lacks, so an undeclared dep is a release pipeline that dies at 'Generate",
    "app icons'."
  ],
  "devDependencies": {
    "@adminium/config": "workspace:*",
    "@adminium/dashboard": "workspace:*",
    "@adminium/tokens": "workspace:*",
    "@electron/rebuild": "^4.2.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "22.15.3",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "eslint": "catalog:",
    "icon-gen": "^5.0.0",
    "sharp": "^0.35.3",
    "typescript": "catalog:",
    "vite": "^7.0.5",
    "vitest": "catalog:"
  }
}
