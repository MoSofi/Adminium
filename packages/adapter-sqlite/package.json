{
  "name": "@adminium/adapter-sqlite",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "SQLite adapter",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@adminium/engine": "workspace:*",
    "better-sqlite3": "^12.2.0",
    "kysely": "^0.28.2"
  },
  "devDependencies": {
    "@adminium/config": "workspace:*",
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "catalog:",
    "eslint": "catalog:",
    "vitest": "catalog:"
  }
}
