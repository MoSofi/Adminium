// SPDX-License-Identifier: AGPL-3.0-only
/**
 * `page-calendar` template (09-generated-app.md §7.6; 04 §10) — the component
 * the dashboard PageRenderer mounts for `template: 'page-calendar'` envelopes.
 */
export {
  PAGE_CALENDAR_TEMPLATE_ID,
  PageCalendar,
  calendarEventsOf,
  calendarItemConfigOf,
  type PageCalendarLabels,
  type PageCalendarProps,
} from './PageCalendar.js';
