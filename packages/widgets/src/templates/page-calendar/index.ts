// SPDX-License-Identifier: AGPL-3.0-only
/**
 * `page-calendar` template — the component the dashboard PageRenderer mounts
 * for `template: 'page-calendar'` envelopes.
 */
export {
  PAGE_CALENDAR_TEMPLATE_ID,
  PageCalendar,
  calendarEventsOf,
  calendarItemConfigOf,
  type PageCalendarLabels,
  type PageCalendarProps,
} from './PageCalendar.js';
