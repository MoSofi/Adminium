/**
 * `page-master-detail` template (09-generated-app.md §7.3; 04 §10) — the
 * component the dashboard PageRenderer mounts for
 * `template: 'page-master-detail'` envelopes: master rail + filter chips +
 * selection-driven detail pane (key-value or domain card) + activity slot.
 */
export {
  PAGE_MASTER_DETAIL_TEMPLATE_ID,
  PageMasterDetail,
  deriveStatusSubtitle,
  type PageMasterDetailLabels,
  type PageMasterDetailProps,
} from './PageMasterDetail.js';
