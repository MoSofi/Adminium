---
'@adminium/manifest': patch
'@adminium/server': patch
---

A sample row can say `"@onlyIfEmpty": true` — an app's own settings row is added only when the operator has none, and never stops the add or takes theirs over.
