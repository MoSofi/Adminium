---
'@adminium/server': patch
'@adminium/widgets': patch
'@adminium/dashboard': patch
'@adminium/i18n': patch
---

A page link's filters can count days from today (`gte:today-30`) and compare a time to now (`before:now`), and every list template — inbox, master-detail, directory, board, calendar, scheduler, files and logs — applies them and shows them as chips.
