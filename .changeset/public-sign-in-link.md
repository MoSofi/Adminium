---
'@adminium/server': patch
'@adminium/meta': patch
'@adminium/public-client': patch
'@adminium/i18n': patch
---

An app's clients can sign in by an emailed link, open a row shared by a link's code, read rows only as far as their parent is theirs, and download their own private files; the public client gains the link, shared-link, file, documents and add-on settings calls.
