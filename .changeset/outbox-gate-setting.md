---
'@adminium/manifest': patch
---

An outbox producer may be switched off by a bool of the app's settings row
of its own (`gate: {setting}`), so each notice a studio receives can have its
own switch.
