---
'@adminium/server': patch
---

An app's email can carry a document: a template's `attach` draws (or reuses) the linked row's document and attaches the PDF, or the print copy where the text is not Latin; a message whose document cannot be drawn fails with the reason and is never sent without it.
