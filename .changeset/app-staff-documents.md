---
'@adminium/server': patch
---

An app's own staff screen can ask for a document of a row it can read (`POST /apps/:key/documents/render`), a staff statement takes its period, and an app ships document profiles for its own tables — made only while the add-on that draws them is attached.
