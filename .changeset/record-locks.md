---
'@adminium/dashboard': patch
'@adminium/server': patch
'@adminium/i18n': patch
---

The record page draws a locked row read-only and hides Delete where the row cannot be deleted; Studio keeps and shows a date's bounds; reminders are dated by an add-on's default ladder until one is saved.
